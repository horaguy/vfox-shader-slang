#!/usr/bin/env bash
#MISE description="Run plugin tests - install and execute tool"
set -euo pipefail

mise plugin link --force shader-slang .
mise cache clear
mise install shader-slang

# Capture and display the output while also verifying it
output=$(mise exec "shader-slang" -- "slangc" --version)
echo "slangc output: $output"

if echo "$output" | grep -q 'shader-slang@'; then
    echo "✓ Tests passed"
else
    echo "✗ Test failed: expected version shader-slang"
    exit 1
fi
